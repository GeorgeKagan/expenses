angular.module('expensesApp').factory('Expense', () => {
    "use strict";

    let service = {};

    /**
     * Add a new expense to the DB.
     * @param data
     */
    service.addNewExpense = data => {

    };

    return service;
});